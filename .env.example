# Production site URL (used for sitemap.xml and robots.txt at build time)
VITE_SITE_URL=https://your-domain.com

# Netlify API — only needed to run: npm run fix:netlify-dns
# Create at: Netlify → User settings → Applications → Personal access tokens
NETLIFY_AUTH_TOKEN=
